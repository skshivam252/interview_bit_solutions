vector<int> Solution::solve(vector<int> &A) {
    vector<int> res;
    for(int i=0;i<A.size();i++){
        if(A[i]!=0){
            res.push_back(A[i]);
        }
    }
    while(res.size()!=A.size()){
        res.push_back(0);
    }
    
    return res;
}

